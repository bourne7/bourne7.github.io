1. install xcode
2. install Command Line Tools (CLT) for Xcode: xcode-select --install
3. install brew following https://brew.sh/ 
```
/usr/bin/ruby -e "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/master/install)"
```
Deafult location is /usr/local, which should include nothing in a new system.


修改 ~/.bash_profile (注意这里和Ubuntu是不同的。Ubuntu使用的是 bashrc)
alias ll="ls -haLlFG"
